import React, {Component} from 'react';
import '../styles/Grid.css';

export default class Grid extends Component {
    render () {
        return (
            <div className="grid">

            </div>
        );
    }
}